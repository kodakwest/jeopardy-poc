rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // POC: open access with auth
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
